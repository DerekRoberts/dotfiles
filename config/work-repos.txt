# Work repos — one org/repo per line (SSH URLs, no trailing slashes)
# clone-work-repos.sh expands each to: git clone git@github.com:ORG/REPO ~/Repos/REPO
# Add more repos below and re-run: scripts/clone-work-repos.sh

bcgov/quickstart-openshift
bcgov/actions
bcgov/actions-openshift
derekroberts/dotfiles
miniontech/vexilon
