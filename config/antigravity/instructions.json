{
  "global_rules": [
    "Default to Gemini 3 Flash for all terminal explanations.",
    "Only use Claude Opus 4.6 when explicitly requested for 'Deep Logic Review'.",
    "Never perform background indexing on directories containing more than 500 files.",
    "Always use standard POSIX commands to ensure compatibility with Fedora 43."
  ]
}
